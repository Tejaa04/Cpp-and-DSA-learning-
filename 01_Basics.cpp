//#include<bits/stdc++.h>  - this is used to import all the libraries in c++
#include<iostream>
using namespace std;    //without this, we need to use std on every function.
int main(){
    cout << "Hello World!" << endl;      //It moves the cursor to the next line after printing.
    cout << "This is my first C++ code.";
    int x, y;           //we can declare the variables at time using ,
    cin >> x >> y;      //to take the input of two declared variables in a single line.
    cout << "The value of x is: " << x << " and y is: "  << y;
}